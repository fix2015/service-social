{
  "name": "service-social",
  "version": "1.0.0",
  "description": "Reusable social microservice: likes, comments, follows, blocks, saves",
  "main": "src/index.js",
  "scripts": {
    "start": "npx prisma migrate deploy && node src/index.js",
    "dev": "node --watch src/index.js",
    "db:migrate": "npx prisma migrate dev"
  },
  "dependencies": {
    "@prisma/client": "^6.6.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "prisma": "^6.6.0"
  }
}
